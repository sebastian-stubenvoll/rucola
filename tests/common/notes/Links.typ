This is a #link("./Booksold") that shouldn't get parsed.
Neither should this plaintext #tag("link") to http://www.google.com

However this one should: #refnote("./Birds.typ", "title")
This one too: #refnote("./Warbler.typ", "title")

#tag("typst")
